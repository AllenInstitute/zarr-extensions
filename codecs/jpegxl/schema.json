{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "const": "jpegxl"
    },
    "configuration": {
      "type": "object",
      "description": "Encoder hints only; decoders MUST ignore these and derive everything from the codestream.",
      "properties": {
        "effort": {
          "type": "integer",
          "minimum": 1,
          "maximum": 10,
          "description": "Encoder effort/speed setting (ignored on decode)."
        },
        "distance": {
          "type": "number",
          "minimum": 0,
          "description": "Butteraugli distance; 0 means mathematically lossless (ignored on decode)."
        },
        "lossless": {
          "type": "boolean",
          "description": "Whether the codestream was encoded losslessly (ignored on decode)."
        }
      },
      "additionalProperties": false
    }
  },
  "required": ["name", "configuration"],
  "additionalProperties": false
}
